#Projeto do Desafio Técnico Python da Technotech

Candidato: Lukas Maximo Grilo Abreu Jardim

##Descrição

O projeto consiste de um projeto Django, contendo conexões com banco de dados SQLite (por enquanto, posteiormente postegresSQL), os arquivos Dockerfile, docker-compose.yml, e meu_database.sql (esse último dentro da pasta test_codes) será comitado na raiz do repositório do github para contornar a restrição do .gitignore para arquivos SQL.

O template em HTML está sendo desenvolvido em um projeto front-end React/TS + Vite, que já tenho familiaridade devido a um processo seletivo anterior que realizei.

As tranzações e funcionalidades já estão todas configuradas e executáveis por meio do django my admin e do meu superusuário:

```
usuário: LukasJardim
senha: spiritchuva
```

As respostas e relatórios estão funcionanando, mais ainda estou descobrindo como conectar os arquivos html do template HTML em React/Typescript ao banco de dados do projeto. Assim que conseguir, pretendo fazer o mesmo com o banco de dados Postgres quando reconfigurar o progeto, mas o principal já está funcionando.

Testei o meu_database.sql no sqlite e já está funcionando, representando as querys no banco de dados do projeto.